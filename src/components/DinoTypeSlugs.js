


   export const DinoTypeSlugs = (props) => {
       if (props.url === 'Triassic') {
          return 1
      } else if (props.url === 'Jurassic') {
          return 2
      } else if (props.url === 'Cretaceous') {
          return 3
      }
    }
